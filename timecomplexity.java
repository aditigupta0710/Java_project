public class timecomplexity {
    //O(1) = constant time

    public static void constanttime 
        System.out.println(arr[0]); // only one operation regardless of input size
    }
    //O(n) -  liner type
    public static void linertime(int[] arr) {
        for(int i = 0; i <= arr.length; i++) {
            System.out.println(arr[i]); // runs n times
        }
    }  
}